escape ^Xx
vbell off
bind w windowlist -b

bind n next
bind p prev
bind k kill

startup_message off

shell "/bin/zsh"
term xterm-256color

hardstatus alwayslastline "%`%-w%{=b bw}%n %t%{-}%+w"
altscreen on
